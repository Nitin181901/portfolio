  <style>
    /* Custom styles to override Bootstrap styles */
    @media (max-width: 767px) {
      nav.navbar .navbar-collapse {
        display: flex !important; /* Override the default display: none; for mobile */
        flex-direction: column; /* Stack items vertically */
      }

      nav.navbar .navbar-nav {
        width: 100%;
        text-align: left; /* Align the links to the left */
      }

      nav.navbar .navbar-nav .nav-item {
        width: 100%;
      }

      .navbar-nav-side-by-side {
        display: flex;
        flex-direction: row;
        justify-content: space-between;
      }
    }

    nav.navbar {
      background-color: #333 !important; 
      --bs-navbar-padding-x: 0;
      --bs-navbar-padding-y: 0rem;
    }

    nav.navbar .navbar-brand,
    nav.navbar .navbar-nav .nav-link,
    nav.navbar .navbar-text {
      color: #fff !important;
    }
  </style>
<body>
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
      <a class="navbar-brand" href="<%= articles_path %>">Nitin's Portfolio</a>
      <div class="collapse navbar-collapse" id="navbarText">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0 navbar-nav-side-by-side">
          <li class="nav-item">
            <a class="nav-link" href="<%= about_me_index_path %>">Past-Work</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="<%= skills_path %>">Skills</a>
          </li>
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="<%= projects_path %>">Projects</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>